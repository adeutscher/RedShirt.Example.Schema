name: Build verification

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  push:
    branches:
      - develop

jobs:
  build:
    if: github.event_name == 'push' || github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore RedShirt.Example.Schema.slnx

      - name: Build
        run: dotnet build RedShirt.Example.Schema.slnx --no-restore --configuration Release

      - name: Test
        run: dotnet test RedShirt.Example.Schema.slnx --no-build --configuration Release --verbosity normal
